<ContentDialog
    x:Class="Astro.Views.Dialogs.CuriosityPhotoDetailsDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Astro.Views.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Windows.UI.Xaml.Controls"
    Title="Curiosity Rover Photo Details"
    Background="{ThemeResource SystemControlChromeHighAcrylicElementMediumBrush}"
    CornerRadius="4"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    PrimaryButtonText="Button1"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
    SecondaryButtonText="Close"
    mc:Ignorable="d">
    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">800</x:Double>
        <x:Double x:Key="ContentDialogMaxHeight">800</x:Double>
    </ContentDialog.Resources>

    <Grid>
        <!--<Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock Text="Curiosity Rover" />
        </Grid>
        <Grid Grid.Row="1">
            <Image Source="{x:Bind CurrentPhoto.img_src}" />
        </Grid>-->
        <muxc:FlipView Background="Transparent" ItemsSource="{x:Bind Photos}">
            <muxc:FlipView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="500" />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Image
                            Grid.Row="0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Source="{Binding img_src}"
                            Stretch="Uniform" />
                        <Grid Grid.Row="1" Margin="24,24,24,0">
                            <Border BorderBrush="LightGray" BorderThickness="0,.5,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel
                                        Grid.Column="0"
                                        Margin="0,12,0,0"
                                        Orientation="Horizontal">
                                        <TextBlock Margin="0,0,4,0" Text="Image taken on: " />
                                        <TextBlock Margin="0" Text="{Binding earth_date}" />
                                        <TextBlock Margin="2,0,2,0" Text="(Sol " />
                                        <TextBlock Margin="2,0,0,0" Text="{Binding sol}" />
                                        <TextBlock Margin="0,0,2,0" Text=")" />
                                    </StackPanel>
                                    <StackPanel
                                        Grid.Column="1"
                                        Margin="0,12,0,0"
                                        HorizontalAlignment="Right"
                                        Orientation="Vertical">
                                        <HyperlinkButton Content="Download Image" />
                                        <HyperlinkButton Content="View Original" NavigateUri="{Binding img_src}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </muxc:FlipView.ItemTemplate>
        </muxc:FlipView>
        <!--<controls:Carousel
            InvertPositive="True"
            ItemDepth="250"
            ItemMargin="0"
            ItemRotationX="0"
            ItemRotationY="45"
            ItemRotationZ="0"
            ItemsSource="{x:Bind Photos}"
            Orientation="Horizontal">
            <controls:Carousel.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </controls:Carousel.EasingFunction>
            <controls:Carousel.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <Image
                                Width="500"
                                Height="500"
                                Source="{Binding img_src}"
                                Stretch="UniformToFill" />
                        </Grid>
                        <Grid Grid.Row="1">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="0,0,2,0" Text="Captured on Sol" />
                                <TextBlock Margin="0,0,2,0" Text="{Binding sol}" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </controls:Carousel.ItemTemplate>
        </controls:Carousel>-->
    </Grid>
</ContentDialog>
