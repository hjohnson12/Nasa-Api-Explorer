<Page
    x:Class="Astro.Views.OpportunityRoverPhotosView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Astro.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Windows.UI.Xaml.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource SystemControlChromeHighAcrylicElementMediumBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Margin="12,0,0,12" Orientation="Vertical">
            <TextBlock
                FontSize="24"
                FontWeight="Bold"
                Text="Curiosity Rover Photos" />
            <muxc:CalendarDatePicker
                x:Name="RoverPhotosDatePicker"
                BorderThickness="1"
                CornerRadius="2"
                DateChanged="RoverPhotosDatePicker_DateChanged"
                Header="Date:" />
            <!--<Button Content="Next" Click="BtnNext_Click" />
            <Button Content="Previous" Click="BtnPrev_Click" />-->
        </StackPanel>

        <GridView
            x:Name="GridViewControl"
            Grid.Row="1"
            Margin="12,0,0,0"
            IsItemClickEnabled="True"
            ItemClick="GridViewControl_ItemClick"
            ItemsSource="{x:Bind OpportunityPhotos, Mode=OneWay}">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid CornerRadius="2">
                        <Grid.Background>
                            <SolidColorBrush Color="{Binding Color}" />
                        </Grid.Background>
                        <Image Source="{Binding img_src}" Stretch="Uniform" />
                        <Border
                            VerticalAlignment="Bottom"
                            Background="#44000000"
                            CornerRadius="2">
                            <TextBlock
                                Margin="3,1"
                                Foreground="White"
                                TextWrapping="Wrap">
                                <Run Text="{Binding id}" /><Run Text="." />
                                <Run Text="{Binding camera.full_name}" />
                            </TextBlock>
                        </Border>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <controls:StaggeredPanel
                        HorizontalAlignment="Left"
                        ColumnSpacing="5"
                        DesiredColumnWidth="250"
                        RowSpacing="5" />
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
        </GridView>
    </Grid>
</Page>
